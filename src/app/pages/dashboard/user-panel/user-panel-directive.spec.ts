import { UserPanelDirective } from './user-panel-directive';

describe('UserPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new UserPanelDirective();
    expect(directive).toBeTruthy();
  });
});
